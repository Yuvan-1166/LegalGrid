{
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14"
  },
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND -c blue,green \"npm run frontend\" \"npm run backend\"",
    "frontend": "cd frontend && npm run dev",
    "backend": "nodemon --watch backend --ext py --exec \"cd backend && source .venv/bin/activate && python main.py\""
  }
}
